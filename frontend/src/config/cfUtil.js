
const lsPrefix = 'boat_log_'; // prefix for local storage

export default lsPrefix;

export const recentLog = lsPrefix+"recLog";

export const recentPage = lsPrefix+"recPage";

export const recentSummary = lsPrefix+"recSum";